const test = require('ava')
const CaptchaSDK = require('..')
const config = require('./config.json')

test(async t => {
  const sdk = new CaptchaSDK(config.appId, config.appSecret)
  const resp = await sdk.verifyToken(config.token)

  t.true(resp.result)
})